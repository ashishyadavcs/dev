import Chat from "@/components/chat";

const Page = () => {
    return (
        <div className="container" style={{ minHeight: "80vh" }}>
            <Chat />
        </div>
    );
};

export default Page;
